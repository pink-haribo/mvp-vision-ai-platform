# Model & Task UX Improvement Plan

**작성일**: 2025-10-31
**목적**: 모델/태스크 증가에 따른 UX 복잡도 완화 및 사용자 경험 개선

---

## 문제 정의

### 1. 모델 선택의 어려움
- **현상**: 모델이 많아지면서 사용자가 어떤 모델을 선택해야 할지 모름
- **원인**:
  - 같은 태스크에 여러 모델 존재 (ResNet-18/50, YOLOv8n/s/m, ViT 등)
  - 성능/속도 트레이드오프 정보 부족
  - Deprecated 모델과 최신 모델 구분 없음
- **영향**:
  - 초보 사용자 이탈
  - 비효율적인 모델 선택 (구형 모델 사용)
  - 지원 부담 증가

### 2. 학습 vs 추론 전용 모델 혼재
- **현상**: 추론 전용 모델(Super-Resolution, Pretrained ViT)과 학습 가능 모델이 같은 흐름
- **원인**:
  - 모델 메타데이터에 `trainable` 속성 없음
  - UI에서 학습/추론 진입점 구분 없음
- **영향**:
  - 사용자가 SR 모델로 학습 시도 → 에러 발생
  - 혼란스러운 UX (학습 불가능한데 학습 UI 진입)

### 3. 테스트/추론 화면 중복 및 비효율
- **현상**:
  - Classification, Detection, Segmentation, SR 등 각 태스크마다 별도 UI
  - 코드 중복 (이미지 업로드, 결과 표시 로직)
  - 일관성 없는 UX
- **원인**:
  - 태스크별로 독립적으로 개발
  - 공통 컴포넌트 부재
- **영향**:
  - 유지보수 어려움 (한 곳 수정하면 여러 곳 수정 필요)
  - 새 태스크 추가 시 개발 비용 증가
  - 사용자 학습 비용 증가 (태스크마다 다른 UI)

---

## 해결 방안

## Part 1: 모델 Deprecation 시스템

### 1.1 모델 메타데이터 확장

**모델 레지스트리에 추가할 필드**:

```python
# mvp/training/model_registry/timm_models.py 예시
TIMM_MODEL_REGISTRY = {
    "resnet50": {
        # 기존 필드...
        "display_name": "ResNet-50",
        "params": "25.6M",

        # 🆕 새로 추가되는 필드
        "status": "active",  # active | deprecated | experimental
        "inference_only": False,  # 학습 불가 여부
        "recommended": True,  # 추천 모델 여부 (UI에서 먼저 표시)
        "deprecation_info": None,  # deprecated일 때 표시할 메시지

        # 🆕 모델 성능 레이블 (사용자 선택 가이드)
        "performance_tier": "balanced",  # fast | balanced | accurate
        "use_case_tags": ["general", "production"],  # general | research | production | experimental

        # 🆕 대체 모델 추천
        "alternatives": {
            "faster": "efficientnet_b0",
            "more_accurate": "resnet152",
            "newer": "efficientnetv2_s"
        }
    },

    "resnet18": {
        # ...
        "status": "deprecated",  # ⚠️ Deprecated 예시
        "deprecation_info": {
            "reason": "ResNet-50 또는 EfficientNet 사용을 권장합니다.",
            "deprecated_since": "2025-10-01",
            "alternative": "efficientnet_b0"  # 대체 모델
        },
        "recommended": False
    }
}
```

**HuggingFace 모델 예시** (Super-Resolution):
```python
"caidas/swin2SR-classical-sr-x2-64": {
    "display_name": "Swin2SR (2x)",
    "status": "active",
    "inference_only": True,  # 🔥 학습 불가, 추론 전용
    "recommended": True,
    "performance_tier": "accurate",
    "use_case_tags": ["production", "image-quality"]
}
```

### 1.2 관리자 인터페이스 (Admin Panel)

**새 API 엔드포인트**:
```
POST /api/v1/admin/models/deprecate
  body: {
    framework: "timm",
    model_name: "resnet18",
    deprecation_info: {
      reason: "더 나은 대안이 있습니다",
      alternative: "efficientnet_b0"
    }
  }

POST /api/v1/admin/models/recommend
  body: {
    framework: "timm",
    model_name: "efficientnet_b0",
    recommended: true
  }
```

**관리자 UI** (`/admin/models`):
```
┌─────────────────────────────────────────────────────┐
│ 모델 관리                                            │
├─────────────────────────────────────────────────────┤
│ [Framework: All ▼] [Task: All ▼] [Status: All ▼]   │
├─────────────────────────────────────────────────────┤
│ Model               Status    Recommended  Actions  │
│ resnet50            ✅ Active  ⭐ Yes       [Edit]   │
│ resnet18            ⚠️ Deprecated  No      [Edit]   │
│ efficientnet_b0     ✅ Active  ⭐ Yes       [Edit]   │
│ swin2SR-x2          ✅ Active  ⭐ Yes       [Edit]   │
│   └─ 🔒 Inference Only                              │
└─────────────────────────────────────────────────────┘
```

### 1.3 프론트엔드 필터링

**사용자 모델 선택 UI**:

```tsx
// 기본: Recommended 모델만 표시
<ModelSelector
  defaultFilter="recommended"  // recommended | all | deprecated
  showDeprecated={false}
/>

// Advanced 모드: 모든 모델 표시 (deprecated 포함)
<Toggle>
  [ ] 모든 모델 표시 (deprecated 포함)
</Toggle>
```

**표시 우선순위**:
1. **Recommended + Active** (기본 표시)
2. Active (non-recommended)
3. Experimental (경고 뱃지)
4. **Deprecated** (숨김, 토글로 표시 가능)

---

## Part 2: 학습/추론 전용 모델 구분 및 UX 흐름

### 2.1 사용자 여정(User Journey) 분리

**현재 (문제)**:
```
사용자 → "새 학습 작업" → 모든 모델 표시 (SR 포함)
                          → SR 선택 → 학습 실패 ❌
```

**개선안**:
```
사용자 → 목적 선택
         ├─ "모델 학습하기" → 학습 가능 모델만 표시
         └─ "사전학습 모델 추론" → 추론 전용 + 학습 가능 모델 모두 표시
```

### 2.2 진입점 분리

**왼쪽 사이드바 재구성**:

```
현재:
┌──────────────────┐
│ 📊 대시보드       │
│ 💬 채팅          │
│ 🎯 학습 관리     │ ← 학습/추론 혼재
│ 📁 데이터셋      │
│ ⚙️  설정         │
└──────────────────┘

개선안:
┌──────────────────┐
│ 📊 대시보드       │
│ 💬 채팅          │
│                  │
│ === 학습 ===     │
│ 🎯 새 학습 작업  │ ← 학습 가능 모델만
│ 📂 내 모델       │
│                  │
│ === 추론 ===     │
│ 🔮 AI 추론       │ ← 추론 전용 (SR, Pretrained)
│ 📁 데이터셋      │
│ ⚙️  설정         │
└──────────────────┘
```

### 2.3 "AI 추론" 페이지 설계

**목적**: Pretrained 모델로 즉시 추론 (학습 없이)

**UI 구조**:
```
┌────────────────────────────────────────────────────┐
│ 🔮 AI 추론                                          │
├────────────────────────────────────────────────────┤
│                                                    │
│ 1. 작업 선택                                        │
│ ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐      │
│ │  이미지 │ │  객체   │ │  배경   │ │  화질   │      │
│ │  분류   │ │  탐지   │ │  제거   │ │ 향상    │      │
│ └────────┘ └────────┘ └────────┘ └────────┘      │
│                                                    │
│ 2. 모델 선택 (자동 추천)                            │
│ ┌─────────────────────────────────────────┐       │
│ │ ⭐ 추천: Swin2SR 2x Upscaling            │       │
│ │ 빠름 ●●●○○ | 정확도 ●●●●○                │       │
│ │ [선택]  [다른 모델 보기]                  │       │
│ └─────────────────────────────────────────┘       │
│                                                    │
│ 3. 이미지 업로드                                    │
│ [파일 선택] 또는 드래그&드롭                         │
│                                                    │
│ [추론 시작]                                        │
└────────────────────────────────────────────────────┘
```

### 2.4 백엔드 API 분리

```python
# 새 엔드포인트
GET /api/v1/models/list?trainable=true   # 학습 가능 모델만
GET /api/v1/models/list?inference_only=true  # 추론 전용 모델만

# 기존 학습 API는 학습 가능 모델만 허용
POST /api/v1/training/jobs
  → inference_only=True 모델 거부

# 새 추론 전용 API
POST /api/v1/inference/quick
  → 모든 모델 허용, 학습 과정 없이 즉시 추론
```

---

## Part 3: 통합 테스트/추론 화면 설계

### 3.1 컴포넌트 아키텍처

**현재 문제**:
- `TestInferencePanel.tsx` - 모든 태스크 하나의 파일에
- 태스크별 분기문 (`if task === 'classification'...`)
- 중복 코드 (업로드, 결과 표시)

**개선안: Composition Pattern**

```tsx
// 1. 공통 레이아웃
<InferenceLayout
  header={<InferenceHeader />}
  uploader={<ImageUploader />}
  results={<InferenceResults taskType={taskType} />}
  sidebar={<InferenceSidebar />}
/>

// 2. 태스크별 결과 렌더러
<InferenceResults taskType="classification">
  <ClassificationResult data={result} />
</InferenceResults>

<InferenceResults taskType="semantic_segmentation">
  <SegmentationResult data={result} />
</InferenceResults>

// 3. 플러그인 시스템
const TASK_RENDERERS = {
  classification: ClassificationResultRenderer,
  object_detection: DetectionResultRenderer,
  semantic_segmentation: SegmentationResultRenderer,
  super_resolution: SuperResolutionResultRenderer,
};

const ResultRenderer = TASK_RENDERERS[taskType];
return <ResultRenderer data={result} />;
```

### 3.2 통합 추론 패널 구조

**파일 구조**:
```
frontend/components/inference/
├── InferencePanel.tsx           # 메인 컨테이너
├── InferenceLayout.tsx          # 공통 레이아웃
├── InferenceUploader.tsx        # 이미지 업로드
├── InferenceResults.tsx         # 결과 디스패처
│
├── renderers/                   # 태스크별 렌더러
│   ├── ClassificationRenderer.tsx
│   ├── DetectionRenderer.tsx
│   ├── SegmentationRenderer.tsx
│   └── SuperResolutionRenderer.tsx
│
├── shared/                      # 공통 컴포넌트
│   ├── ImageGrid.tsx
│   ├── ResultCard.tsx
│   └── MetricsDisplay.tsx
│
└── hooks/
    ├── useInference.ts          # 추론 로직
    └── useTaskConfig.ts         # 태스크별 설정
```

**InferencePanel.tsx** (Simplified):
```tsx
export function InferencePanel({ taskType, jobId }: Props) {
  const { config } = useTaskConfig(taskType);
  const { uploadImage, runInference, results } = useInference(jobId);

  return (
    <InferenceLayout>
      <InferenceHeader
        title={config.title}
        description={config.description}
      />

      <InferenceUploader
        accept={config.acceptedFormats}
        onUpload={uploadImage}
      />

      <InferenceResults
        taskType={taskType}
        results={results}
        renderer={config.renderer}
      />
    </InferenceLayout>
  );
}
```

### 3.3 태스크별 설정 시스템

**useTaskConfig.ts**:
```tsx
const TASK_CONFIGS = {
  classification: {
    title: "이미지 분류 추론",
    description: "이미지를 여러 클래스 중 하나로 분류합니다",
    acceptedFormats: "image/*",
    renderer: ClassificationRenderer,
    resultFields: ["predicted_label", "confidence", "top5"],
    visualizations: ["confidence_chart"],
  },

  semantic_segmentation: {
    title: "시맨틱 분할 추론",
    description: "이미지의 모든 픽셀을 클래스별로 분할합니다",
    acceptedFormats: "image/*",
    renderer: SegmentationRenderer,
    resultFields: ["predicted_mask_url", "overlay_url", "num_classes"],
    visualizations: ["segmentation_map", "overlay", "class_distribution"],
  },

  super_resolution: {
    title: "초해상화 추론",
    description: "저해상도 이미지를 고해상도로 업스케일합니다",
    acceptedFormats: "image/*",
    renderer: SuperResolutionRenderer,
    resultFields: ["upscaled_image_url", "scale_factor"],
    visualizations: ["before_after_slider", "zoom_comparison"],
  },
};

export function useTaskConfig(taskType: string) {
  return { config: TASK_CONFIGS[taskType] };
}
```

### 3.4 공통 UI 패턴

**모든 태스크에 공통 적용**:

1. **이미지 업로드 영역** (동일)
   ```
   ┌─────────────────────────────┐
   │  📷 이미지를 드래그하거나    │
   │     클릭하여 업로드          │
   │                             │
   │  [파일 선택]                │
   └─────────────────────────────┘
   ```

2. **결과 카드 레이아웃** (동일)
   ```
   ┌──────────────┬──────────────┐
   │  원본 이미지  │  결과 정보    │
   │  (썸네일)    │  (태스크별)   │
   │              │              │
   │  [상세보기]   │  [다운로드]   │
   └──────────────┴──────────────┘
   ```

3. **메트릭 표시** (태스크별 다름)
   - Classification: Top-5 Confidence Bar Chart
   - Detection: mAP, Detected Objects Count
   - Segmentation: Class Distribution Pie Chart
   - SR: PSNR, SSIM (if available)

---

## 구현 우선순위

### Phase 1: 모델 메타데이터 확장 (1-2일)
- [ ] 모델 레지스트리에 `status`, `inference_only`, `recommended` 필드 추가
- [ ] 기존 모델에 메타데이터 채우기
- [ ] `/models/list` API에 필터링 파라미터 추가

### Phase 2: 프론트엔드 필터링 (1-2일)
- [ ] ModelSelector 컴포넌트에 필터링 로직 추가
- [ ] Recommended 모델 우선 표시
- [ ] Deprecated 모델 숨김/표시 토글

### Phase 3: 진입점 분리 (2-3일)
- [ ] 사이드바 재구성 (학습/추론 섹션 분리)
- [ ] "AI 추론" 페이지 신규 생성
- [ ] 학습 API에서 `inference_only` 모델 거부 로직

### Phase 4: 통합 추론 패널 (3-4일)
- [ ] `InferenceLayout`, `InferenceResults` 공통 컴포넌트
- [ ] 태스크별 렌더러 분리 (ClassificationRenderer, SegmentationRenderer 등)
- [ ] `useTaskConfig` 훅으로 태스크 설정 중앙화
- [ ] 기존 TestInferencePanel 리팩토링

### Phase 5: 관리자 인터페이스 (2-3일)
- [ ] `/admin/models` 페이지 생성
- [ ] Deprecate/Recommend API 구현
- [ ] 모델 상태 관리 UI

---

## 예상 효과

### 사용자 경험
- ✅ 모델 선택 혼란 감소 (추천 모델만 표시)
- ✅ 명확한 학습/추론 흐름 구분
- ✅ 일관된 UI/UX (모든 태스크 동일한 패턴)

### 개발 효율
- ✅ 새 태스크 추가 비용 감소 (렌더러만 추가)
- ✅ 코드 중복 제거
- ✅ 유지보수 용이

### 운영 효율
- ✅ 관리자가 모델 큐레이션 가능
- ✅ 구형 모델 점진적 폐기 가능
- ✅ 사용자 피드백 기반 추천 모델 조정

---

## 주요 의사결정 사항

### Q1: Deprecated 모델을 완전히 제거할 것인가?
**결정**: 아니오. 숨김 처리 후 토글로 표시 가능하게.
**이유**:
- 기존 학습 작업에서 여전히 사용 중일 수 있음
- 호환성 유지 필요

### Q2: 추론 전용 모델도 학습 작업에 포함할 것인가?
**결정**: 아니오. 학습 작업 생성 시 차단.
**이유**:
- 사용자 혼란 방지
- 에러 사전 차단

### Q3: 모든 태스크를 하나의 InferencePanel에?
**결정**: 예. 태스크별 렌더러로 분기.
**이유**:
- 코드 중복 제거
- 일관된 UX
- 새 태스크 추가 용이

---

## 다음 단계

1. **현재 코드 분석**
   - 현재 모델 레지스트리 구조 확인
   - TestInferencePanel 코드 리뷰
   - 중복 코드 식별

2. **프로토타입 구현**
   - 모델 메타데이터 확장 (timm 1-2개 모델)
   - InferenceRenderer 패턴 검증 (classification + segmentation)
   - UI 목업 작성

3. **사용자 테스트**
   - 내부 테스트로 UX 검증
   - 피드백 수집 후 조정

4. **전체 마이그레이션**
   - 모든 모델에 메타데이터 추가
   - 모든 태스크 통합 패널로 마이그레이션
   - 관리자 인터페이스 구현

---

**문서 작성**: Claude Code
**검토 필요**: UX 흐름, 컴포넌트 구조, API 설계
