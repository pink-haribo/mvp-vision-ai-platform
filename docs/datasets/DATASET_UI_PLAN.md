# 데이터셋 관리 UI 구현 계획

**작성일**: 2025-11-03
**목표**: 사용자 친화적인 데이터셋 생성, 업로드, 관리 화면

---

## 1. UI 배치 및 네비게이션

### 1.1 Sidebar 버튼 위치
```
┌─ Sidebar ────────────┐
│ Vision AI Platform   │
├──────────────────────┤
│ 이미지 도구          │  ← 기존
│ 📦 데이터셋         │  ← ✨ 새로 추가
│ 프로젝트            │  ← 기존
├──────────────────────┤
│ [프로젝트 목록]      │
│ ...                  │
└──────────────────────┘
```

**변경 사항**:
- `components/Sidebar.tsx`: "데이터셋" 버튼 추가 (Database 아이콘)
- `app/page.tsx`: `showDatasets` state 및 핸들러 추가

### 1.2 Workspace 영역
```
┌─ Sidebar ─┬─ Chat Panel ─┬─ Workspace (Datasets) ────────────┐
│            │               │                                    │
│            │               │  [데이터셋 관리 화면]              │
│            │               │                                    │
└────────────┴───────────────┴────────────────────────────────────┘
```

**레이아웃**: 기존 ImageToolsPanel, AdminProjectsPanel과 동일한 전체 너비 사용

---

## 2. UI 구조 (2-Column Layout)

### 2.1 전체 레이아웃
```
┌─────────────────────────────────────────────────────────────────┐
│ Header: 데이터셋                                [+ 새 데이터셋]  │
├──────────────────┬──────────────────────────────────────────────┤
│                  │                                               │
│  Left Sidebar    │  Right Panel (Detail/Upload)                 │
│  (List)          │                                               │
│                  │                                               │
│  ┌─────────────┐ │  ┌──────────────────────────────────────┐   │
│  │ 내 데이터셋 │ │  │                                      │   │
│  │ 공개 데이터셋│ │  │  선택된 데이터셋 상세                │   │
│  └─────────────┘ │  │  또는                                │   │
│                  │  │  새 데이터셋 업로드                  │   │
│  [검색]          │  │                                      │   │
│                  │  │                                      │   │
│  ┌─────────────┐ │  └──────────────────────────────────────┘   │
│  │ Dataset 1   │ │                                               │
│  │ 1000 images │ │                                               │
│  └─────────────┘ │                                               │
│  ┌─────────────┐ │                                               │
│  │ Dataset 2   │ │                                               │
│  └─────────────┘ │                                               │
│                  │                                               │
└──────────────────┴──────────────────────────────────────────────┘
```

### 2.2 Left Sidebar (Dataset List)

**탭 구조**:
```
┌─────────────────────┐
│ [내 데이터셋] [공개] │
├─────────────────────┤
│ 🔍 [검색...]        │
├─────────────────────┤
│ ┌─────────────────┐ │
│ │ 📁 My Dataset   │ │  ← 카드 형태
│ │ 1,200 images    │ │
│ │ ImageFolder     │ │
│ │ Private         │ │
│ └─────────────────┘ │
│                     │
│ ┌─────────────────┐ │
│ │ 📁 Custom Data  │ │
│ │ 500 images      │ │
│ │ YOLO            │ │
│ │ Private         │ │
│ └─────────────────┘ │
└─────────────────────┘
```

**카드 정보**:
- 데이터셋 이름
- 이미지 개수
- 포맷 (ImageFolder, YOLO, COCO)
- Visibility (Private, Public, Organization)
- 선택 시 하이라이트

### 2.3 Right Panel (3가지 상태)

#### 상태 1: 아무것도 선택 안 됨 (Empty State)
```
┌─────────────────────────────────────┐
│                                     │
│        📦                           │
│    데이터셋 선택                    │
│                                     │
│  왼쪽에서 데이터셋을 선택하거나     │
│  새 데이터셋을 생성하세요          │
│                                     │
└─────────────────────────────────────┘
```

#### 상태 2: 데이터셋 상세 보기
```
┌─────────────────────────────────────────────────────┐
│ ← 뒤로    My Custom Dataset            [수정] [삭제]│
├─────────────────────────────────────────────────────┤
│                                                      │
│ 📋 기본 정보                                        │
│ ────────────────────────────────────────────────    │
│ 이름: My Custom Dataset                             │
│ 설명: My custom training data for object detection │
│ 포맷: YOLO                                          │
│ Task Type: Object Detection                         │
│                                                      │
│ 📊 통계                                             │
│ ────────────────────────────────────────────────    │
│ 이미지 수: 1,200                                    │
│ 클래스 수: 10                                       │
│ 클래스: cat, dog, person, car, ...                 │
│ 총 용량: 2.5 GB                                     │
│                                                      │
│ 📁 스토리지                                         │
│ ────────────────────────────────────────────────    │
│ 타입: R2                                            │
│ 경로: datasets/550e8400-.../                        │
│ Content Hash: a3f2b8c9d1e5...                       │
│ Version: 1                                          │
│                                                      │
│ 👥 권한 (Owner만 표시)                              │
│ ────────────────────────────────────────────────    │
│ [+ 팀원 초대]                                       │
│                                                      │
│ 📎 액션                                             │
│ ────────────────────────────────────────────────    │
│ [📤 파일 추가 업로드]  [🏋️ 학습 시작]              │
│                                                      │
└─────────────────────────────────────────────────────┘
```

#### 상태 3: 새 데이터셋 업로드
```
┌─────────────────────────────────────────────────────┐
│ ← 뒤로    새 데이터셋 생성                          │
├─────────────────────────────────────────────────────┤
│                                                      │
│ Step 1: 기본 정보                                   │
│ ────────────────────────────────────────────────    │
│ 데이터셋 이름 *                                     │
│ [                                                 ] │
│                                                      │
│ 설명                                                │
│ [                                                 ] │
│ [                                                 ] │
│                                                      │
│ Visibility                                          │
│ ○ Private    ○ Public    ○ Organization            │
│                                                      │
│ Tags (선택)                                         │
│ [custom, object-detection                        ] │
│                                                      │
│ ────────────────────────────────────────────────    │
│                                                      │
│ Step 2: 파일 업로드                                │
│ ────────────────────────────────────────────────    │
│                                                      │
│ ┌───────────────────────────────────────────────┐  │
│ │                                               │  │
│ │           📁                                  │  │
│ │   드래그 앤 드롭 또는 클릭하여 파일 선택     │  │
│ │                                               │  │
│ │   지원 형식: .zip, .tar.gz, 폴더             │  │
│ │   최대 크기: 10GB                             │  │
│ │                                               │  │
│ └───────────────────────────────────────────────┘  │
│                                                      │
│ 선택된 파일: dataset.zip (1.2 GB)                   │
│                                                      │
│ ⚡ 자동 분석 결과                                   │
│ ────────────────────────────────────────────────    │
│ ✓ 형식: YOLO (신뢰도: 98%)                          │
│ ✓ Task Type: Object Detection                       │
│ ✓ 클래스: 10개 (cat, dog, person, ...)             │
│ ✓ 이미지: 1,200장                                   │
│ ⚠️ 경고: 클래스 불균형 (ratio: 2.3)                │
│                                                      │
│ ────────────────────────────────────────────────    │
│                        [취소]  [업로드 및 생성]     │
└─────────────────────────────────────────────────────┘
```

---

## 3. 기능 정의

### 3.1 필수 기능 (Phase 1)

#### F1. 데이터셋 목록 조회
- **API**: `GET /api/v1/datasets/available`
- **UI**: Left Sidebar에 카드 리스트
- **필터**: "내 데이터셋" / "공개 데이터셋" 탭
- **검색**: 이름, 설명, ID로 검색
- **정렬**: 이름, 생성일, 이미지 수

#### F2. 데이터셋 상세 보기
- **API**: `GET /api/v1/datasets/{id}` (⬜ 구현 필요)
- **UI**: Right Panel에 상세 정보 표시
- **정보**:
  - 기본 정보 (이름, 설명, 포맷, task_type)
  - 통계 (이미지 수, 클래스 수, 클래스 이름)
  - 스토리지 (경로, content_hash, version)
  - 권한 (Owner인 경우만)

#### F3. 새 데이터셋 생성 (업로드)
- **API**: `POST /api/v1/datasets/upload` (✅ 구현됨, UI만)
- **UI**: Right Panel에 업로드 폼
- **단계**:
  1. 기본 정보 입력 (이름, 설명, visibility, tags)
  2. 파일 업로드 (드래그 앤 드롭 또는 파일 선택)
  3. 자동 분석 (형식, 클래스, 이미지 수)
  4. 확인 및 생성
- **지원 형식**: `.zip`, `.tar.gz`, 폴더
- **진행률**: 업로드 진행률 표시

#### F4. 데이터셋 삭제
- **API**: `DELETE /api/v1/datasets/{id}` (⬜ 구현 필요)
- **UI**: 상세 화면에 삭제 버튼
- **확인 모달**: "정말 삭제하시겠습니까?"
- **옵션**: R2 스토리지도 함께 삭제 (체크박스)
- **권한**: Owner만

### 3.2 추가 기능 (Phase 2)

#### F5. 데이터셋 메타데이터 수정
- **API**: `PUT /api/v1/datasets/{id}` (⬜ 구현 필요)
- **UI**: 수정 모드 (편집 가능한 필드)
- **수정 가능**: 이름, 설명, tags, visibility
- **권한**: Owner, Editor

#### F6. 파일 추가 업로드 (점진적 업로드)
- **API**: `POST /api/v1/datasets/{id}/upload` (⬜ 구현 필요)
- **UI**: 상세 화면에 "파일 추가 업로드" 버튼
- **옵션**: 이미지만, 레이블만, 전체
- **시나리오**: Day 1: 100장 → Day 2: +50장
- **권한**: Owner, Editor

#### F7. 권한 관리 (팀원 초대)
- **API**:
  - `GET /api/v1/datasets/{id}/permissions` (⬜ 구현 필요)
  - `POST /api/v1/datasets/{id}/permissions` (⬜ 구현 필요)
- **UI**: 권한 관리 섹션 (Owner만 표시)
- **기능**: 팀원 이메일로 초대, 역할 선택 (Viewer, Editor)
- **권한**: Owner만

#### F8. 학습 시작 연동
- **UI**: 상세 화면에 "학습 시작" 버튼
- **동작**: TrainingConfigPanel로 이동 + dataset_id 자동 입력
- **권한**: Owner, Editor, Viewer

---

## 4. 컴포넌트 구조

### 4.1 파일 구조
```
mvp/frontend/components/
├── datasets/
│   ├── DatasetWorkspace.tsx         ← 메인 컴포넌트 (2-column layout)
│   ├── DatasetListSidebar.tsx       ← Left Sidebar (목록)
│   ├── DatasetDetailPanel.tsx       ← Right Panel (상세)
│   ├── DatasetUploadPanel.tsx       ← Right Panel (업로드)
│   ├── DatasetEmptyState.tsx        ← Right Panel (empty)
│   ├── DatasetListCard.tsx          ← 리스트 카드 아이템
│   ├── DatasetDeleteModal.tsx       ← 삭제 확인 모달
│   ├── DatasetPermissionPanel.tsx   ← 권한 관리 (Phase 2)
│   └── index.ts                     ← Export
│
├── Sidebar.tsx                       ← 수정: 데이터셋 버튼 추가
└── ...
```

### 4.2 컴포넌트 관계
```
DatasetWorkspace (메인)
├── DatasetListSidebar (좌측)
│   ├── 탭: 내 데이터셋 / 공개 데이터셋
│   ├── 검색 바
│   └── DatasetListCard[] (카드 리스트)
│
└── Right Panel (우측, 조건부 렌더링)
    ├── DatasetEmptyState (선택 안 됨)
    ├── DatasetDetailPanel (상세 보기)
    │   ├── 기본 정보
    │   ├── 통계
    │   ├── 스토리지 정보
    │   ├── 권한 (Owner만)
    │   └── 액션 버튼들
    └── DatasetUploadPanel (새 업로드)
        ├── Step 1: 기본 정보
        ├── Step 2: 파일 업로드
        ├── Step 3: 자동 분석 결과
        └── 버튼: 취소 / 업로드
```

### 4.3 State 관리
```typescript
// DatasetWorkspace.tsx
const [datasets, setDatasets] = useState<Dataset[]>([])
const [selectedDatasetId, setSelectedDatasetId] = useState<string | null>(null)
const [isCreatingNew, setIsCreatingNew] = useState(false)
const [activeTab, setActiveTab] = useState<'my' | 'public'>('my')
const [searchQuery, setSearchQuery] = useState('')
```

---

## 5. API 요구사항

### 5.1 즉시 필요 (Phase 1)
- ⬜ `GET /api/v1/datasets/{id}` - 상세 조회
- ⬜ `DELETE /api/v1/datasets/{id}` - 삭제
- ✅ `POST /api/v1/datasets/upload` - 업로드 (구현됨)

### 5.2 추후 필요 (Phase 2)
- ⬜ `PUT /api/v1/datasets/{id}` - 메타데이터 수정
- ⬜ `POST /api/v1/datasets/{id}/upload` - 파일 추가
- ⬜ `GET /api/v1/datasets/{id}/permissions` - 권한 조회
- ⬜ `POST /api/v1/datasets/{id}/permissions` - 권한 부여

---

## 6. UI 스타일 가이드 (기존 일관성 유지)

### 6.1 색상 팔레트
```css
/* Primary */
--indigo-600: #4F46E5
--indigo-700: #4338CA

/* Background */
--gray-50: #F9FAFB
--gray-100: #F3F4F6
--gray-900: #111827

/* Text */
--gray-600: #4B5563
--gray-900: #111827

/* Borders */
--gray-200: #E5E7EB
```

### 6.2 타이포그래피
- **Header**: `text-2xl font-bold text-gray-900`
- **Section Title**: `text-lg font-semibold text-gray-900`
- **Body**: `text-sm text-gray-600`
- **Label**: `text-xs font-semibold text-gray-700 uppercase tracking-wider`

### 6.3 버튼 스타일
```tsx
// Primary
className="px-4 py-2 bg-indigo-600 text-white rounded-lg hover:bg-indigo-700"

// Secondary
className="px-4 py-2 bg-white border border-gray-300 text-gray-700 rounded-lg hover:bg-gray-50"

// Danger
className="px-4 py-2 bg-red-600 text-white rounded-lg hover:bg-red-700"
```

### 6.4 카드 스타일
```tsx
// 기본 카드
className="p-4 bg-white border border-gray-200 rounded-lg hover:border-indigo-300 cursor-pointer transition-colors"

// 선택된 카드
className="p-4 bg-indigo-50 border-2 border-indigo-500 rounded-lg shadow-md"
```

### 6.5 아이콘
```tsx
import { Database, Upload, Trash2, Edit2, Users, Download } from 'lucide-react'
```

---

## 7. 드래그 앤 드롭 구현

### 7.1 라이브러리
- **react-dropzone** 사용 권장
- 또는 Native HTML5 Drag and Drop API

### 7.2 기능
- 파일 드래그 앤 드롭
- 클릭하여 파일 선택
- 파일 타입 검증 (.zip, .tar.gz, 이미지 폴더)
- 파일 크기 제한 (최대 10GB)
- 진행률 표시

### 7.3 예시 코드
```typescript
import { useDropzone } from 'react-dropzone'

const { getRootProps, getInputProps, isDragActive } = useDropzone({
  accept: {
    'application/zip': ['.zip'],
    'application/gzip': ['.tar.gz'],
    'application/x-tar': ['.tar'],
  },
  maxSize: 10 * 1024 * 1024 * 1024, // 10GB
  onDrop: (acceptedFiles) => {
    // Handle files
  }
})
```

---

## 8. 구현 우선순위

### Phase 1A: 기본 UI 및 목록 (1-2일)
**목표**: 데이터셋 목록 조회 및 Empty State

**Tasks**:
1. ⬜ Sidebar에 "데이터셋" 버튼 추가
2. ⬜ DatasetWorkspace 메인 컴포넌트 생성 (2-column layout)
3. ⬜ DatasetListSidebar 구현 (탭, 검색, 카드 리스트)
4. ⬜ DatasetListCard 구현
5. ⬜ DatasetEmptyState 구현
6. ⬜ app/page.tsx에 통합

**Success Criteria**:
- 사이드바에서 "데이터셋" 클릭 시 화면 전환
- 내 데이터셋 / 공개 데이터셋 탭 동작
- 검색 기능 동작
- 빈 상태 UI 표시

---

### Phase 1B: 상세 보기 및 삭제 (1-2일)
**목표**: 데이터셋 상세 정보 표시 및 삭제

**Tasks**:
1. ⬜ `GET /api/v1/datasets/{id}` API 구현 (Backend)
2. ⬜ `DELETE /api/v1/datasets/{id}` API 구현 (Backend)
3. ⬜ DatasetDetailPanel 구현
4. ⬜ DatasetDeleteModal 구현
5. ⬜ 상세 정보 섹션들 (기본 정보, 통계, 스토리지)

**Success Criteria**:
- 데이터셋 클릭 시 상세 정보 표시
- 삭제 버튼 클릭 시 확인 모달
- 삭제 완료 시 목록에서 제거

---

### Phase 1C: 업로드 (2-3일)
**목표**: 새 데이터셋 생성 및 업로드

**Tasks**:
1. ⬜ DatasetUploadPanel 구현
2. ⬜ 드래그 앤 드롭 기능 (react-dropzone)
3. ⬜ 진행률 표시 (WebSocket 또는 polling)
4. ⬜ 자동 분석 결과 표시
5. ⬜ 업로드 완료 후 목록 갱신

**Success Criteria**:
- "새 데이터셋" 버튼 클릭 시 업로드 화면
- 파일 드래그 앤 드롭 동작
- 업로드 진행률 표시
- 자동 분석 결과 표시
- 업로드 완료 시 목록에 추가

---

### Phase 2: 수정 및 권한 관리 (3-4일)
**목표**: 메타데이터 수정, 파일 추가, 권한 관리

**Tasks**:
1. ⬜ `PUT /api/v1/datasets/{id}` API 구현
2. ⬜ `POST /api/v1/datasets/{id}/upload` API 구현
3. ⬜ `GET/POST /api/v1/datasets/{id}/permissions` API 구현
4. ⬜ 수정 모드 UI
5. ⬜ 파일 추가 업로드 UI
6. ⬜ DatasetPermissionPanel 구현

**Success Criteria**:
- 메타데이터 수정 가능
- 기존 데이터셋에 파일 추가 가능
- 팀원 초대 및 권한 부여 가능

---

## 9. 테스트 시나리오

### 9.1 기본 플로우
1. 사이드바에서 "데이터셋" 클릭
2. "새 데이터셋" 버튼 클릭
3. 기본 정보 입력 (이름, 설명, visibility)
4. zip 파일 드래그 앤 드롭
5. 자동 분석 결과 확인
6. "업로드 및 생성" 클릭
7. 진행률 표시 확인
8. 완료 후 목록에서 확인
9. 데이터셋 클릭하여 상세 보기
10. "삭제" 버튼으로 삭제

### 9.2 점진적 업로드 (Phase 2)
1. 기존 데이터셋 선택
2. "파일 추가 업로드" 클릭
3. 추가 파일 업로드
4. 메타데이터 자동 갱신 확인
5. 이미지 수 증가 확인

### 9.3 권한 관리 (Phase 2)
1. Owner 계정으로 데이터셋 선택
2. "팀원 초대" 클릭
3. 이메일 입력 및 역할 선택
4. 권한 부여 확인
5. 다른 계정으로 로그인
6. 공유된 데이터셋 확인

---

## 10. 다음 단계

**즉시 착수**:
1. Sidebar에 "데이터셋" 버튼 추가
2. DatasetWorkspace 메인 레이아웃 생성
3. DatasetListSidebar 구현

**이번 주 내**:
4. DatasetDetailPanel 구현
5. Backend API (GET/DELETE) 구현
6. DatasetUploadPanel 구현

**다음 주**:
7. 수정 및 권한 관리 기능

---

**Last Updated**: 2025-11-03
**Status**: Ready for Implementation
