# ==========================================
# Backend API
# ==========================================
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1
NEXT_PUBLIC_STORAGE_ENDPOINT=http://localhost:30900

# ==========================================
# NextAuth.js Configuration
# ==========================================
# NextAuth URL (must match your frontend URL)
NEXTAUTH_URL=http://localhost:3000

# NextAuth Secret (generate with: openssl rand -base64 32)
NEXTAUTH_SECRET=your-nextauth-secret-change-this-in-production

# ==========================================
# Keycloak OIDC Configuration
# ==========================================
# Keycloak Client ID (Frontend client - public)
KEYCLOAK_CLIENT_ID=platform-frontend

# Keycloak Client Secret (leave empty for public clients)
KEYCLOAK_CLIENT_SECRET=

# Keycloak Issuer URL (Realm endpoint - for browser)
KEYCLOAK_ISSUER=http://localhost:8080/realms/vision-ai

# Keycloak Issuer Internal URL (for server-side token exchange)
# K8s: Use service name (e.g., http://keycloak:8080/realms/vision-ai)
# Local: Same as KEYCLOAK_ISSUER or use HTTP to avoid SSL errors
KEYCLOAK_ISSUER_INTERNAL=http://localhost:8080/realms/vision-ai

# ==========================================
# Development: Disable TLS verification (⚠️ DEV ONLY!)
# ==========================================
# Uncomment if using self-signed certificates in development
# NODE_TLS_REJECT_UNAUTHORIZED=0
