# ================================
# Frontend Environment Variables
# ================================
#
# Setup Instructions:
#   1. Copy this file: cp .env.example .env.local
#   2. Update URLs to match your backend configuration
#   3. Start frontend: pnpm dev

# ==========================================
# Backend API Configuration
# ==========================================
# Backend API base URL
# Local Dev: http://localhost:8000/api/v1
# Production: https://your-api-domain.com/api/v1
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1

# ==========================================
# Storage Configuration
# ==========================================
# Object storage endpoint for direct file access
# Local Dev: MinIO on port 9000
# Production: S3, R2, or managed MinIO
NEXT_PUBLIC_STORAGE_ENDPOINT=http://localhost:9000

# ==========================================
# Observability Tools (Optional)
# ==========================================
# ClearML Web UI URL (Phase 12.2+)
# Local Dev: localhost:8080 (docker-compose)
# Production: Managed ClearML or self-hosted
NEXT_PUBLIC_CLEARML_WEB_URL=http://localhost:8080

# MLflow Web UI URL (Optional - if using mlflow backend)
# Local Dev: localhost:5000 (docker-compose)
# Production: Managed MLflow or self-hosted
NEXT_PUBLIC_MLFLOW_URL=http://localhost:5000

# Grafana Dashboard URL (Phase 13+)
# Local Dev: localhost:3200 (docker-compose)
# Production: Managed Grafana or self-hosted
NEXT_PUBLIC_GRAFANA_URL=http://localhost:3200

# ==========================================
# Labeler Service (Phase 11.5+)
# ==========================================
# Labeler backend URL for SSO (Backend, not Frontend!)
# Local Dev: localhost:8011 (Labeler Backend)
# Production: https://labeler-api.your-domain.com
NEXT_PUBLIC_LABELER_URL=http://localhost:8011
