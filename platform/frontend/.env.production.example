# Production Environment Variables Template
# Set these in Railway dashboard Variables tab

# ==========================================
# Backend API URLs (REQUIRED)
# ==========================================
# Replace <backend-url> with your Railway backend service URL
# Example: https://backend-production-xxxx.up.railway.app
NEXT_PUBLIC_API_URL=https://<backend-url>/api/v1
NEXT_PUBLIC_WS_URL=wss://<backend-url>/ws

# ==========================================
# Storage (Cloudflare R2 / S3)
# ==========================================
# Replace with your R2/S3 public endpoint URL
# Example: https://pub-xxxxx.r2.dev
NEXT_PUBLIC_STORAGE_ENDPOINT=https://<storage-endpoint-url>

# ==========================================
# Application
# ==========================================
NEXT_PUBLIC_ENVIRONMENT=production

# ==========================================
# NextAuth.js (REQUIRED)
# ==========================================
# Your production frontend URL
NEXTAUTH_URL=https://your-domain.com

# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=your-production-secret-replace-this

# ==========================================
# Keycloak SSO (REQUIRED)
# ==========================================
# Public URL for browser redirects
KEYCLOAK_ISSUER=https://auth.company.com/realms/vision-ai

# Internal URL for server-side token exchange (K8s service name)
KEYCLOAK_ISSUER_INTERNAL=http://keycloak:8080/realms/vision-ai

# Keycloak Client ID (Frontend - public client)
KEYCLOAK_CLIENT_ID=platform-frontend

# Keycloak Client Secret (leave empty for public clients)
KEYCLOAK_CLIENT_SECRET=
