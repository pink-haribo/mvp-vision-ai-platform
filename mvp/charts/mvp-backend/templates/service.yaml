apiVersion: v1
kind: Service
metadata:
  name: {{ include "mvp-backend.fullname" . }}
  labels:
    {{- include "mvp-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mvp-backend.selectorLabels" . | nindent 4 }}
