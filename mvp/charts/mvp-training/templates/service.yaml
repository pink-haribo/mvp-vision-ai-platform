apiVersion: v1
kind: Service
metadata:
  name: {{ include "mvp-training.fullname" . }}
  labels:
    {{- include "mvp-training.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mvp-training.selectorLabels" . | nindent 4 }}
