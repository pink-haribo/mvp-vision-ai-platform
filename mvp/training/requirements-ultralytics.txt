# ultralytics Service Dependencies
# Object Detection, Segmentation, Pose: YOLO family

# Web Framework (API Server)
fastapi==0.108.0
uvicorn[standard]==0.25.0
python-multipart==0.0.6

# Deep Learning Framework
# Install with: pip install torch==2.1.1 torchvision==0.16.1 --index-url https://download.pytorch.org/whl/cpu
# Using CPU version to avoid Windows CUDA DLL loading issues
torch==2.1.1
torchvision==0.16.1

# Model Library
ultralytics>=8.3.0

# Common
numpy<2.0,>=1.23.0  # MLflow requires <2.0, ultralytics requires >=1.23.0
pillow==10.1.0
opencv-python==4.8.1.78  # Use opencv-python instead of headless for ultralytics compatibility

# S3/R2 Storage
boto3==1.34.19

# MLflow for experiment tracking
mlflow==2.9.2

# Requests for HTTP API calls (TrainingLogger)
requests==2.31.0

# Configuration & Data Validation
pydantic==2.5.0
python-dotenv==1.0.0
pyyaml>=6.0

# Database
psycopg2-binary==2.9.9  # PostgreSQL driver for Railway

# Validation Metrics
scikit-learn>=1.3.0
