# MVP 구현 계획

## 🎯 MVP 목표

**핵심 가치 검증:**
"사용자가 자연어로 Vision 모델 학습을 설정하고 실행할 수 있는가?"

**검증할 가설:**
1. LLM이 자연어를 학습 설정으로 정확하게 파싱할 수 있는가?
2. 파싱된 설정으로 실제 학습이 정상 실행되는가?
3. 사용자가 학습 진행상황을 실시간으로 확인할 수 있는가?
4. End-to-End 플로우가 원활하게 동작하는가?

**목표 기간:** 2주 (10 영업일)

---

## 📦 MVP 범위

### ✅ 포함 기능 (Must Have)

1. **자연어 입력 및 파싱**
   - 간단한 채팅 UI
   - LLM으로 의도 파싱 (Claude/GPT)
   - 기본 질문-답변으로 정보 수집

2. **학습 설정**
   - 1개 모델만 지원: ResNet50 (timm)
   - 1개 태스크만 지원: Image Classification
   - 간단한 데이터셋 업로드 (ImageFolder 형식)

3. **학습 실행**
   - 로컬 프로세스로 실행 (Subprocess)
   - 기본 하이퍼파라미터만 지원 (epochs, batch_size, lr)
   - stdout 파싱으로 진행률 추적

4. **실시간 모니터링**
   - WebSocket으로 진행률 전송
   - 간단한 Progress Bar + 메트릭 표시
   - 학습 완료 알림

5. **결과 확인**
   - 최종 accuracy/loss 표시
   - 학습 완료된 모델 다운로드

### ❌ 제외 기능 (나중에)

- 다중 모델 프레임워크 (HuggingFace, YOLO 등)
- Object Detection, Segmentation
- Temporal 워크플로우
- Kubernetes 클러스터
- 분산 학습
- Auto-scaling
- 추론 API 배포
- 사용자 인증/권한
- 프로젝트 관리
- 데이터셋 검증/변환
- Cost estimation

---

## 🏗️ 간소화된 아키텍처

```
┌─────────────────────────────────────────┐
│         Frontend (Next.js)               │
│  - Chat UI (간단한 입력창)               │
│  - Progress Panel (실시간 업데이트)      │
└─────────────────┬───────────────────────┘
                  │ HTTP + WebSocket
                  ↓
┌─────────────────────────────────────────┐
│       Backend (FastAPI - 단일 서비스)    │
│  - /chat/message  (LLM 파싱)            │
│  - /training/start (학습 시작)           │
│  - /ws (WebSocket)                       │
└─────────────────┬───────────────────────┘
                  │
         ┌────────┴────────┐
         ↓                 ↓
┌─────────────────┐  ┌─────────────────┐
│  SQLite DB      │  │ Training Process │
│  - sessions     │  │ (Subprocess)     │
│  - workflows    │  │                  │
└─────────────────┘  └─────────────────┘
```

**변경 사항:**
- 마이크로서비스 → **단일 FastAPI 서비스**
- PostgreSQL/MongoDB → **SQLite**
- Temporal → **Python subprocess**
- Kubernetes → **로컬 프로세스**
- S3 → **로컬 파일시스템**
- Redis → **Python dict (in-memory)**

---

## 📁 MVP 폴더 구조

```
mvp-vision-platform/
├── frontend/                  # Next.js
│   ├── app/
│   │   ├── page.tsx          # 메인 페이지 (채팅 + 학습 진행)
│   │   └── api/
│   ├── components/
│   │   ├── ChatPanel.tsx     # 채팅 UI
│   │   └── TrainingPanel.tsx # 학습 진행 표시
│   └── lib/
│       ├── api.ts            # API 클라이언트
│       └── websocket.ts      # WebSocket 연결
│
├── backend/                   # FastAPI (단일 서비스)
│   ├── main.py               # FastAPI 앱
│   ├── api/
│   │   ├── chat.py           # 채팅 엔드포인트
│   │   ├── training.py       # 학습 엔드포인트
│   │   └── websocket.py      # WebSocket
│   ├── services/
│   │   ├── llm_parser.py     # LLM 파싱
│   │   └── trainer.py        # 학습 실행
│   ├── models/               # SQLAlchemy 모델
│   └── database.py           # DB 연결
│
├── training/                  # 학습 스크립트
│   └── train_classification.py  # ResNet 학습
│
├── uploads/                   # 업로드된 데이터셋
├── outputs/                   # 학습 결과
├── mvp.db                     # SQLite DB
├── .env                       # 환경 변수
└── requirements.txt           # Python 패키지
```

---

## 🗓️ 2주 구현 일정

### Week 1: 기반 구조 + LLM 파싱

#### Day 1-2: 프로젝트 셋업
- [ ] 폴더 구조 생성
- [ ] Frontend 초기화 (Next.js)
- [ ] Backend 초기화 (FastAPI)
- [ ] SQLite 데이터베이스 모델 정의
- [ ] 환경 변수 설정

**산출물:**
- 실행 가능한 Hello World (Frontend + Backend)
- DB 테이블 생성 완료

#### Day 3-4: LLM 파싱 구현
- [ ] LangChain + Anthropic Claude 연동
- [ ] Intent 파싱 프롬프트 작성
- [ ] 자연어 → TrainingConfig 변환
- [ ] 간단한 대화 플로우 (질문-답변)

**산출물:**
- "ResNet50으로 고양이 3종류 분류" → Config 생성
- 부족한 정보 질문 생성

#### Day 5: Frontend 채팅 UI
- [ ] 채팅 입력창 + 메시지 리스트
- [ ] API 연동
- [ ] 파싱 결과 표시

**산출물:**
- 동작하는 채팅 인터페이스

### Week 2: 학습 실행 + 모니터링

#### Day 6-7: 학습 스크립트 구현
- [ ] PyTorch + timm ResNet50 학습 코드
- [ ] ImageFolder 데이터셋 로더
- [ ] stdout으로 진행률 출력 (Epoch, Loss, Accuracy)
- [ ] 모델 저장

**산출물:**
- 커맨드라인에서 실행 가능한 학습 스크립트
- `python train.py --data ./data --epochs 10`

#### Day 8: 학습 실행 연동
- [ ] FastAPI에서 subprocess로 학습 실행
- [ ] stdout 실시간 파싱
- [ ] 진행률 추출 및 저장

**산출물:**
- API로 학습 시작 가능
- 진행률 실시간 추적

#### Day 9: WebSocket + 실시간 UI
- [ ] WebSocket 엔드포인트 구현
- [ ] Frontend WebSocket 연결
- [ ] Progress Bar + 메트릭 표시
- [ ] 학습 완료 알림

**산출물:**
- 실시간 업데이트되는 학습 UI

#### Day 10: 통합 테스트 + 버그 수정
- [ ] End-to-End 테스트
- [ ] 에러 처리 개선
- [ ] UI/UX 다듬기
- [ ] 데모 준비

**산출물:**
- 완전히 동작하는 MVP
- 데모 가능한 상태

---

## 🔧 기술 스택 (MVP)

### Frontend
- **Next.js 14** (App Router)
- **TailwindCSS** (스타일링)
- **Socket.io-client** (WebSocket)
- **Zustand** (간단한 상태 관리)

### Backend
- **FastAPI** (웹 프레임워크)
- **SQLAlchemy** (ORM)
- **SQLite** (데이터베이스)
- **LangChain + Claude** (LLM 파싱)
- **Socket.io** (WebSocket)

### Training
- **PyTorch** (딥러닝 프레임워크)
- **timm** (ResNet 모델)
- **torchvision** (데이터셋 로더)

---

## 📋 MVP 데이터베이스 스키마 (SQLite)

### chat_sessions
```sql
CREATE TABLE chat_sessions (
    id TEXT PRIMARY KEY,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    messages TEXT,  -- JSON 문자열
    parsed_intent TEXT  -- JSON 문자열
);
```

### training_workflows
```sql
CREATE TABLE training_workflows (
    id TEXT PRIMARY KEY,
    session_id TEXT,
    status TEXT DEFAULT 'pending',  -- pending, running, completed, failed
    config TEXT,  -- JSON 문자열
    progress INTEGER DEFAULT 0,
    current_epoch INTEGER,
    total_epochs INTEGER,
    metrics TEXT,  -- JSON 문자열
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    started_at TIMESTAMP,
    completed_at TIMESTAMP,
    error_message TEXT,
    output_dir TEXT
);
```

---

## 🎨 MVP UI 와이어프레임

### 메인 페이지 (단일 페이지)

```
┌─────────────────────────────────────────────────────────────┐
│ Vision AI Training Platform (MVP)                           │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  ┌───────────────────────────┐  ┌──────────────────────────┐│
│  │   Chat Panel              │  │  Training Status          ││
│  │                           │  │                           ││
│  │  You: ResNet50으로        │  │  Status: Running          ││
│  │       고양이 분류 모델     │  │                           ││
│  │                           │  │  Progress: 45/100 epochs  ││
│  │  AI: 좋아요! 몇 가지      │  │  ███████████░░░░  45%     ││
│  │      확인할게요...        │  │                           ││
│  │      데이터셋은?          │  │  Metrics:                 ││
│  │                           │  │    Loss: 0.234            ││
│  │  You: ./cat_dataset       │  │    Accuracy: 89.2%        ││
│  │                           │  │                           ││
│  │  AI: 설정 완료!           │  │  Time: 00:15:23           ││
│  │      [학습 시작]          │  │  ETA: 00:18:00            ││
│  │                           │  │                           ││
│  │ ─────────────────────     │  │  [Stop Training]          ││
│  │  Type a message...        │  │  [Download Model]         ││
│  └───────────────────────────┘  └──────────────────────────┘│
│                                                               │
└─────────────────────────────────────────────────────────────┘
```

---

## 📝 주요 API 엔드포인트 (MVP)

### POST /api/chat/message
자연어 메시지 전송

**Request:**
```json
{
  "session_id": "sess_123",
  "message": "ResNet50으로 고양이 3종류 분류"
}
```

**Response:**
```json
{
  "type": "clarification",
  "message": "좋아요! 데이터셋은 어디 있나요?",
  "parsed_intent": {
    "model": "resnet50",
    "task": "classification",
    "num_classes": 3
  }
}
```

### POST /api/training/start
학습 시작

**Request:**
```json
{
  "session_id": "sess_123",
  "config": {
    "model": "resnet50",
    "dataset_path": "./uploads/cat_dataset",
    "num_classes": 3,
    "epochs": 100,
    "batch_size": 32,
    "learning_rate": 0.001
  }
}
```

**Response:**
```json
{
  "workflow_id": "wf_789",
  "status": "starting"
}
```

### GET /api/training/{workflow_id}
학습 상태 조회

**Response:**
```json
{
  "workflow_id": "wf_789",
  "status": "running",
  "progress": 45,
  "current_epoch": 45,
  "total_epochs": 100,
  "metrics": {
    "loss": 0.234,
    "accuracy": 0.892
  }
}
```

### WebSocket /ws/{workflow_id}
실시간 업데이트

**Message:**
```json
{
  "type": "progress",
  "epoch": 45,
  "metrics": {
    "loss": 0.234,
    "accuracy": 0.892
  }
}
```

---

## 🧪 테스트 시나리오

### Scenario 1: 해피 패스
1. 사용자: "ResNet50으로 고양이 3종류 분류하는 모델 만들어줘"
2. AI: "좋아요! 데이터셋 경로를 알려주세요"
3. 사용자: "./cat_dataset"
4. AI: "Epochs는 몇 번 할까요?"
5. 사용자: "50번"
6. AI: [설정 완료, 학습 시작 버튼 표시]
7. 사용자: [학습 시작] 클릭
8. → 학습 진행 (실시간 업데이트)
9. → 학습 완료 알림
10. → 모델 다운로드

### Scenario 2: 에러 처리
1. 잘못된 데이터셋 경로 → 에러 메시지 표시
2. 학습 중 중단 → Stop 버튼으로 중단 가능
3. LLM 파싱 실패 → 재시도 유도

---

## 🚀 MVP 실행 방법

### 1. 환경 설정
```bash
# 환경 변수
cat > .env <<EOF
ANTHROPIC_API_KEY=sk-ant-...
DATABASE_URL=sqlite:///./mvp.db
EOF
```

### 2. Backend 실행
```bash
cd backend
pip install -r requirements.txt
python -m uvicorn main:app --reload --port 8000
```

### 3. Frontend 실행
```bash
cd frontend
pnpm install
pnpm dev
```

### 4. 테스트
```bash
# 브라우저에서
http://localhost:3000

# 채팅 입력
"ResNet50으로 고양이 3종류 분류"
```

---

## 📊 성공 지표

### 기술적 지표
- [ ] LLM 파싱 정확도 > 90%
- [ ] 학습 정상 완료율 > 95%
- [ ] WebSocket 연결 안정성 > 99%
- [ ] End-to-End 처리 시간 < 5초 (학습 제외)

### 사용자 경험
- [ ] 자연어 입력부터 학습 시작까지 < 1분
- [ ] 실시간 업데이트 지연 < 1초
- [ ] 직관적인 UI (설명 없이 사용 가능)

---

## 🔄 MVP 이후 확장 계획

### Phase 2 (MVP + 1개월)
- 다중 모델 지원 (VGG, EfficientNet)
- PostgreSQL 마이그레이션
- 사용자 인증
- 프로젝트 관리
- 데이터셋 검증

### Phase 3 (MVP + 2개월)
- Object Detection (YOLO)
- Temporal 워크플로우 도입
- Redis 캐싱
- 추론 API

### Phase 4 (MVP + 3개월)
- Kubernetes 배포
- 분산 학습
- Auto-scaling
- Production ready

---

## ⚠️ MVP 제약사항 및 기술부채

### 알려진 제약사항
1. **단일 모델만 지원**: ResNet50만 가능
2. **로컬 실행만**: 클라우드 배포 불가
3. **동시 학습 불가**: 한 번에 1개만 실행
4. **인증 없음**: 보안 취약
5. **확장성 제한**: SQLite는 동시 쓰기 제한

### 기술부채
1. SQLite → PostgreSQL 마이그레이션 필요
2. Subprocess → Temporal 전환 필요
3. In-memory state → Redis 전환 필요
4. 하드코딩된 설정 → Config 파일화

**중요:** MVP는 검증용입니다. Production에는 적합하지 않습니다.

---

## 📚 참고 자료

- [LangChain 문서](https://python.langchain.com/)
- [FastAPI WebSocket](https://fastapi.tiangolo.com/advanced/websockets/)
- [timm 모델 리스트](https://github.com/huggingface/pytorch-image-models)
- [PyTorch 튜토리얼](https://pytorch.org/tutorials/)

---

## ✅ MVP 체크리스트

### 구현 전
- [ ] 요구사항 명확화
- [ ] 기술 스택 확정
- [ ] 개발 환경 셋업

### 구현 중
- [ ] Day 1-2: 프로젝트 셋업
- [ ] Day 3-4: LLM 파싱
- [ ] Day 5: Frontend UI
- [ ] Day 6-7: 학습 스크립트
- [ ] Day 8: 학습 연동
- [ ] Day 9: WebSocket + 실시간 UI
- [ ] Day 10: 통합 테스트

### 구현 후
- [ ] End-to-End 테스트 통과
- [ ] 데모 준비
- [ ] 문서 작성
- [ ] 다음 단계 계획

---

**MVP 구현을 시작할 준비가 되었습니다! 🚀**

다음 명령어로 시작:
```bash
make mvp-setup  # MVP 프로젝트 구조 생성
make mvp-dev    # MVP 개발 서버 실행
```
